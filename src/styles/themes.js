const LIGHT_THEME = {
  background: 'var(--color-white)',
  foreground: 'var(--color-black)',
  linkColor: 'inherit',
};

const DARK_THEME = {
  background: 'var(--color-black)',
  foreground: 'var(--color-white)',
  linkColor: 'var(--color-blue-200)',
};

export const THEMES = {
  light: LIGHT_THEME,
  dark: DARK_THEME,
};

export const DEFAULT_THEME = 'light';
